name: hotspot
description: Easy product tours.
version: 0.0.1

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: null
